{
  "name": "world-population-dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.css *.js dist/ 2>/dev/null || echo 'No files to copy'",
    "postbuild": "echo 'Build completed'"
  }
}
